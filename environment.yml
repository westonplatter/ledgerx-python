name: ledgerx

channels:
    - default

dependencies:
    - requests
    # dev resources
    - pandas
    # test resources
    - pytest
    - pip
    - pip:
        - Deprecated
        # dev resources
        - twine
        - ipdb
        - "--editable ."
        # testing resources
        - requests_mock