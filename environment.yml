name: ledgerx

channels:
    - default

dependencies:
    - python=3.7
    - requests
    # dev resources
    - pandas
    # test resources
    - pytest
    - pip
    - pip:
        - Deprecated
        # dev resources
        - twine
        - ipdb
        - "--editable ."
        # testing resources